// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pdcp", "for pdcp support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pdcp", "enable pdcp support", "no");

if (PHP_PDCP != "no") {
	EXTENSION("pdcp", "pdcp.c");
}

